package designpatterns.iterator;

import java.util.Iterator;

/**
 * A Range object represents a range of integers based on a given
 * start, end and step.
 * 
 * start = integer to start from (inclusive)
 * end = integer to stop at (not inclusive)
 * step = integer to increment by with each iteration
 */
// e.g. Range(0, 12, 2) would go from 0 to 2 then 4, 6, 8, 10 and stop there (because 12 is not included)
public class Range implements Iterable<Integer> {

	// TODO: Complete this class

}
